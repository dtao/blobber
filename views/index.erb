<!DOCTYPE html>
<html>
  <head>
    <title>Docked</title>

    <style>
      html, body {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
      }

      main {
        position: absolute;
        top: 50%;
        left: 0;
        right: 0;
        height: 500px;
        margin-top: -250px;
        text-align: center;
        font-size: 200%;
      }

      input, textarea {
        padding: 10px;
      }

      input[type="text"], textarea {
        display: block;
        width: 50%;
        margin: 0 auto 10px;
      }

      textarea {
        height: 200px;
      }

      input[type="submit"] {
        min-width: 120px;
      }
    </style>
  </head>

  <body>
    <main>
      <p>All your docs are belong to me.</p>
      <form action="/<%= @id %>" method="POST">
        <input type="hidden" name="token" value="<%= @token %>" />
        <input type="text" name="title" placeholder="Enter a title (optional)" value="<%= @title %>" />
        <textarea name="content" placeholder="Enter literally anything in here"><%= @content %></textarea>
        <input type="submit" value="Store" />
      </form>
    </main>
  </body>
</html>
